import styled from "styled-components";

const Wrapper = styled.div`
  max-width: 700px;
  margin-left: auto;
  margin-right: auto;
`;

const List = styled.ol`
  padding: 0;
  margin: 0;
`;

export { Wrapper, List };
